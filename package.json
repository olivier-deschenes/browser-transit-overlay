{
  "name": "browser-transit-overlay",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "package:extension": "python scripts/package_extension.py"
  }
}
